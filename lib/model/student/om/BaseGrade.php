<?php

/**
 * Base class that represents a row from the 'sch_grade' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Sun Jan 20 11:49:12 2013
 *
 * @package    lib.model.student.om
 */
abstract class BaseGrade extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        GradePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the id_state field.
	 * @var        int
	 */
	protected $id_state;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the description field.
	 * @var        string
	 */
	protected $description;

	/**
	 * The value for the deleted_by field.
	 * @var        int
	 */
	protected $deleted_by;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the degree_id field.
	 * @var        int
	 */
	protected $degree_id;

	/**
	 * The value for the timetable_id field.
	 * @var        int
	 */
	protected $timetable_id;

	/**
	 * The value for the curso_id field.
	 * @var        int
	 */
	protected $curso_id;

	/**
	 * The value for the paralelo_id field.
	 * @var        int
	 */
	protected $paralelo_id;

	/**
	 * @var        Degree
	 */
	protected $aDegree;

	/**
	 * @var        Timetable
	 */
	protected $aTimetable;

	/**
	 * @var        Curso
	 */
	protected $aCurso;

	/**
	 * @var        Paralelo
	 */
	protected $aParalelo;

	/**
	 * @var        array GradeSubject[] Collection to store aggregation of GradeSubject objects.
	 */
	protected $collGradeSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGradeSubjects.
	 */
	private $lastGradeSubjectCriteria = null;

	/**
	 * @var        array ItemGrade[] Collection to store aggregation of ItemGrade objects.
	 */
	protected $collItemGrades;

	/**
	 * @var        Criteria The criteria used to select the current contents of collItemGrades.
	 */
	private $lastItemGradeCriteria = null;

	/**
	 * @var        array ContractGrade[] Collection to store aggregation of ContractGrade objects.
	 */
	protected $collContractGrades;

	/**
	 * @var        Criteria The criteria used to select the current contents of collContractGrades.
	 */
	private $lastContractGradeCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'GradePeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [id_state] column value.
	 * 
	 * @return     int
	 */
	public function getIdState()
	{
		return $this->id_state;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [description] column value.
	 * 
	 * @return     string
	 */
	public function getDescription()
	{
		return $this->description;
	}

	/**
	 * Get the [deleted_by] column value.
	 * 
	 * @return     int
	 */
	public function getDeletedBy()
	{
		return $this->deleted_by;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [degree_id] column value.
	 * 
	 * @return     int
	 */
	public function getDegreeId()
	{
		return $this->degree_id;
	}

	/**
	 * Get the [timetable_id] column value.
	 * 
	 * @return     int
	 */
	public function getTimetableId()
	{
		return $this->timetable_id;
	}

	/**
	 * Get the [curso_id] column value.
	 * 
	 * @return     int
	 */
	public function getCursoId()
	{
		return $this->curso_id;
	}

	/**
	 * Get the [paralelo_id] column value.
	 * 
	 * @return     int
	 */
	public function getParaleloId()
	{
		return $this->paralelo_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = GradePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [id_state] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setIdState($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_state !== $v) {
			$this->id_state = $v;
			$this->modifiedColumns[] = GradePeer::ID_STATE;
		}

		return $this;
	} // setIdState()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = GradePeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [description] column.
	 * 
	 * @param      string $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setDescription($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->description !== $v) {
			$this->description = $v;
			$this->modifiedColumns[] = GradePeer::DESCRIPTION;
		}

		return $this;
	} // setDescription()

	/**
	 * Set the value of [deleted_by] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setDeletedBy($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->deleted_by !== $v) {
			$this->deleted_by = $v;
			$this->modifiedColumns[] = GradePeer::DELETED_BY;
		}

		return $this;
	} // setDeletedBy()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = GradePeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = GradePeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Set the value of [degree_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setDegreeId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->degree_id !== $v) {
			$this->degree_id = $v;
			$this->modifiedColumns[] = GradePeer::DEGREE_ID;
		}

		if ($this->aDegree !== null && $this->aDegree->getId() !== $v) {
			$this->aDegree = null;
		}

		return $this;
	} // setDegreeId()

	/**
	 * Set the value of [timetable_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setTimetableId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->timetable_id !== $v) {
			$this->timetable_id = $v;
			$this->modifiedColumns[] = GradePeer::TIMETABLE_ID;
		}

		if ($this->aTimetable !== null && $this->aTimetable->getId() !== $v) {
			$this->aTimetable = null;
		}

		return $this;
	} // setTimetableId()

	/**
	 * Set the value of [curso_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setCursoId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->curso_id !== $v) {
			$this->curso_id = $v;
			$this->modifiedColumns[] = GradePeer::CURSO_ID;
		}

		if ($this->aCurso !== null && $this->aCurso->getId() !== $v) {
			$this->aCurso = null;
		}

		return $this;
	} // setCursoId()

	/**
	 * Set the value of [paralelo_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Grade The current object (for fluent API support)
	 */
	public function setParaleloId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->paralelo_id !== $v) {
			$this->paralelo_id = $v;
			$this->modifiedColumns[] = GradePeer::PARALELO_ID;
		}

		if ($this->aParalelo !== null && $this->aParalelo->getId() !== $v) {
			$this->aParalelo = null;
		}

		return $this;
	} // setParaleloId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_state = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->name = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->description = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->deleted_by = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->created_at = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->updated_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->degree_id = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->timetable_id = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->curso_id = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->paralelo_id = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 11; // 11 = GradePeer::NUM_COLUMNS - GradePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Grade object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aDegree !== null && $this->degree_id !== $this->aDegree->getId()) {
			$this->aDegree = null;
		}
		if ($this->aTimetable !== null && $this->timetable_id !== $this->aTimetable->getId()) {
			$this->aTimetable = null;
		}
		if ($this->aCurso !== null && $this->curso_id !== $this->aCurso->getId()) {
			$this->aCurso = null;
		}
		if ($this->aParalelo !== null && $this->paralelo_id !== $this->aParalelo->getId()) {
			$this->aParalelo = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GradePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = GradePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aDegree = null;
			$this->aTimetable = null;
			$this->aCurso = null;
			$this->aParalelo = null;
			$this->collGradeSubjects = null;
			$this->lastGradeSubjectCriteria = null;

			$this->collItemGrades = null;
			$this->lastItemGradeCriteria = null;

			$this->collContractGrades = null;
			$this->lastContractGradeCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GradePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseGrade:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				GradePeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseGrade:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GradePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseGrade:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(GradePeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(GradePeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseGrade:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				GradePeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDegree !== null) {
				if ($this->aDegree->isModified() || $this->aDegree->isNew()) {
					$affectedRows += $this->aDegree->save($con);
				}
				$this->setDegree($this->aDegree);
			}

			if ($this->aTimetable !== null) {
				if ($this->aTimetable->isModified() || $this->aTimetable->isNew()) {
					$affectedRows += $this->aTimetable->save($con);
				}
				$this->setTimetable($this->aTimetable);
			}

			if ($this->aCurso !== null) {
				if ($this->aCurso->isModified() || $this->aCurso->isNew()) {
					$affectedRows += $this->aCurso->save($con);
				}
				$this->setCurso($this->aCurso);
			}

			if ($this->aParalelo !== null) {
				if ($this->aParalelo->isModified() || $this->aParalelo->isNew()) {
					$affectedRows += $this->aParalelo->save($con);
				}
				$this->setParalelo($this->aParalelo);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = GradePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = GradePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += GradePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collGradeSubjects !== null) {
				foreach ($this->collGradeSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collItemGrades !== null) {
				foreach ($this->collItemGrades as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collContractGrades !== null) {
				foreach ($this->collContractGrades as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDegree !== null) {
				if (!$this->aDegree->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDegree->getValidationFailures());
				}
			}

			if ($this->aTimetable !== null) {
				if (!$this->aTimetable->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTimetable->getValidationFailures());
				}
			}

			if ($this->aCurso !== null) {
				if (!$this->aCurso->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCurso->getValidationFailures());
				}
			}

			if ($this->aParalelo !== null) {
				if (!$this->aParalelo->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aParalelo->getValidationFailures());
				}
			}


			if (($retval = GradePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collGradeSubjects !== null) {
					foreach ($this->collGradeSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collItemGrades !== null) {
					foreach ($this->collItemGrades as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collContractGrades !== null) {
					foreach ($this->collContractGrades as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = GradePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getIdState();
				break;
			case 2:
				return $this->getName();
				break;
			case 3:
				return $this->getDescription();
				break;
			case 4:
				return $this->getDeletedBy();
				break;
			case 5:
				return $this->getCreatedAt();
				break;
			case 6:
				return $this->getUpdatedAt();
				break;
			case 7:
				return $this->getDegreeId();
				break;
			case 8:
				return $this->getTimetableId();
				break;
			case 9:
				return $this->getCursoId();
				break;
			case 10:
				return $this->getParaleloId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = GradePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getIdState(),
			$keys[2] => $this->getName(),
			$keys[3] => $this->getDescription(),
			$keys[4] => $this->getDeletedBy(),
			$keys[5] => $this->getCreatedAt(),
			$keys[6] => $this->getUpdatedAt(),
			$keys[7] => $this->getDegreeId(),
			$keys[8] => $this->getTimetableId(),
			$keys[9] => $this->getCursoId(),
			$keys[10] => $this->getParaleloId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = GradePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setIdState($value);
				break;
			case 2:
				$this->setName($value);
				break;
			case 3:
				$this->setDescription($value);
				break;
			case 4:
				$this->setDeletedBy($value);
				break;
			case 5:
				$this->setCreatedAt($value);
				break;
			case 6:
				$this->setUpdatedAt($value);
				break;
			case 7:
				$this->setDegreeId($value);
				break;
			case 8:
				$this->setTimetableId($value);
				break;
			case 9:
				$this->setCursoId($value);
				break;
			case 10:
				$this->setParaleloId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = GradePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdState($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setName($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setDescription($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setDeletedBy($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCreatedAt($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setUpdatedAt($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setDegreeId($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setTimetableId($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCursoId($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setParaleloId($arr[$keys[10]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(GradePeer::DATABASE_NAME);

		if ($this->isColumnModified(GradePeer::ID)) $criteria->add(GradePeer::ID, $this->id);
		if ($this->isColumnModified(GradePeer::ID_STATE)) $criteria->add(GradePeer::ID_STATE, $this->id_state);
		if ($this->isColumnModified(GradePeer::NAME)) $criteria->add(GradePeer::NAME, $this->name);
		if ($this->isColumnModified(GradePeer::DESCRIPTION)) $criteria->add(GradePeer::DESCRIPTION, $this->description);
		if ($this->isColumnModified(GradePeer::DELETED_BY)) $criteria->add(GradePeer::DELETED_BY, $this->deleted_by);
		if ($this->isColumnModified(GradePeer::CREATED_AT)) $criteria->add(GradePeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(GradePeer::UPDATED_AT)) $criteria->add(GradePeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(GradePeer::DEGREE_ID)) $criteria->add(GradePeer::DEGREE_ID, $this->degree_id);
		if ($this->isColumnModified(GradePeer::TIMETABLE_ID)) $criteria->add(GradePeer::TIMETABLE_ID, $this->timetable_id);
		if ($this->isColumnModified(GradePeer::CURSO_ID)) $criteria->add(GradePeer::CURSO_ID, $this->curso_id);
		if ($this->isColumnModified(GradePeer::PARALELO_ID)) $criteria->add(GradePeer::PARALELO_ID, $this->paralelo_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(GradePeer::DATABASE_NAME);

		$criteria->add(GradePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Grade (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdState($this->id_state);

		$copyObj->setName($this->name);

		$copyObj->setDescription($this->description);

		$copyObj->setDeletedBy($this->deleted_by);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setDegreeId($this->degree_id);

		$copyObj->setTimetableId($this->timetable_id);

		$copyObj->setCursoId($this->curso_id);

		$copyObj->setParaleloId($this->paralelo_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getGradeSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGradeSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getItemGrades() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addItemGrade($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getContractGrades() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addContractGrade($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Grade Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     GradePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new GradePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Degree object.
	 *
	 * @param      Degree $v
	 * @return     Grade The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDegree(Degree $v = null)
	{
		if ($v === null) {
			$this->setDegreeId(NULL);
		} else {
			$this->setDegreeId($v->getId());
		}

		$this->aDegree = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Degree object, it will not be re-added.
		if ($v !== null) {
			$v->addGrade($this);
		}

		return $this;
	}


	/**
	 * Get the associated Degree object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Degree The associated Degree object.
	 * @throws     PropelException
	 */
	public function getDegree(PropelPDO $con = null)
	{
		if ($this->aDegree === null && ($this->degree_id !== null)) {
			$this->aDegree = DegreePeer::retrieveByPk($this->degree_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDegree->addGrades($this);
			 */
		}
		return $this->aDegree;
	}

	/**
	 * Declares an association between this object and a Timetable object.
	 *
	 * @param      Timetable $v
	 * @return     Grade The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTimetable(Timetable $v = null)
	{
		if ($v === null) {
			$this->setTimetableId(NULL);
		} else {
			$this->setTimetableId($v->getId());
		}

		$this->aTimetable = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Timetable object, it will not be re-added.
		if ($v !== null) {
			$v->addGrade($this);
		}

		return $this;
	}


	/**
	 * Get the associated Timetable object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Timetable The associated Timetable object.
	 * @throws     PropelException
	 */
	public function getTimetable(PropelPDO $con = null)
	{
		if ($this->aTimetable === null && ($this->timetable_id !== null)) {
			$this->aTimetable = TimetablePeer::retrieveByPk($this->timetable_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTimetable->addGrades($this);
			 */
		}
		return $this->aTimetable;
	}

	/**
	 * Declares an association between this object and a Curso object.
	 *
	 * @param      Curso $v
	 * @return     Grade The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCurso(Curso $v = null)
	{
		if ($v === null) {
			$this->setCursoId(NULL);
		} else {
			$this->setCursoId($v->getId());
		}

		$this->aCurso = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Curso object, it will not be re-added.
		if ($v !== null) {
			$v->addGrade($this);
		}

		return $this;
	}


	/**
	 * Get the associated Curso object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Curso The associated Curso object.
	 * @throws     PropelException
	 */
	public function getCurso(PropelPDO $con = null)
	{
		if ($this->aCurso === null && ($this->curso_id !== null)) {
			$this->aCurso = CursoPeer::retrieveByPk($this->curso_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCurso->addGrades($this);
			 */
		}
		return $this->aCurso;
	}

	/**
	 * Declares an association between this object and a Paralelo object.
	 *
	 * @param      Paralelo $v
	 * @return     Grade The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setParalelo(Paralelo $v = null)
	{
		if ($v === null) {
			$this->setParaleloId(NULL);
		} else {
			$this->setParaleloId($v->getId());
		}

		$this->aParalelo = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Paralelo object, it will not be re-added.
		if ($v !== null) {
			$v->addGrade($this);
		}

		return $this;
	}


	/**
	 * Get the associated Paralelo object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Paralelo The associated Paralelo object.
	 * @throws     PropelException
	 */
	public function getParalelo(PropelPDO $con = null)
	{
		if ($this->aParalelo === null && ($this->paralelo_id !== null)) {
			$this->aParalelo = ParaleloPeer::retrieveByPk($this->paralelo_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aParalelo->addGrades($this);
			 */
		}
		return $this->aParalelo;
	}

	/**
	 * Clears out the collGradeSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGradeSubjects()
	 */
	public function clearGradeSubjects()
	{
		$this->collGradeSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGradeSubjects collection (array).
	 *
	 * By default this just sets the collGradeSubjects collection to an empty array (like clearcollGradeSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGradeSubjects()
	{
		$this->collGradeSubjects = array();
	}

	/**
	 * Gets an array of GradeSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Grade has previously been saved, it will retrieve
	 * related GradeSubjects from storage. If this Grade is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array GradeSubject[]
	 * @throws     PropelException
	 */
	public function getGradeSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGradeSubjects === null) {
			if ($this->isNew()) {
			   $this->collGradeSubjects = array();
			} else {

				$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

				GradeSubjectPeer::addSelectColumns($criteria);
				$this->collGradeSubjects = GradeSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

				GradeSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastGradeSubjectCriteria) || !$this->lastGradeSubjectCriteria->equals($criteria)) {
					$this->collGradeSubjects = GradeSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGradeSubjectCriteria = $criteria;
		return $this->collGradeSubjects;
	}

	/**
	 * Returns the number of related GradeSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related GradeSubject objects.
	 * @throws     PropelException
	 */
	public function countGradeSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGradeSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

				$count = GradeSubjectPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

				if (!isset($this->lastGradeSubjectCriteria) || !$this->lastGradeSubjectCriteria->equals($criteria)) {
					$count = GradeSubjectPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGradeSubjects);
				}
			} else {
				$count = count($this->collGradeSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a GradeSubject object to this object
	 * through the GradeSubject foreign key attribute.
	 *
	 * @param      GradeSubject $l GradeSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGradeSubject(GradeSubject $l)
	{
		if ($this->collGradeSubjects === null) {
			$this->initGradeSubjects();
		}
		if (!in_array($l, $this->collGradeSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGradeSubjects, $l);
			$l->setGrade($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Grade is new, it will return
	 * an empty collection; or if this Grade has previously
	 * been saved, it will retrieve related GradeSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Grade.
	 */
	public function getGradeSubjectsJoinSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGradeSubjects === null) {
			if ($this->isNew()) {
				$this->collGradeSubjects = array();
			} else {

				$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

				$this->collGradeSubjects = GradeSubjectPeer::doSelectJoinSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(GradeSubjectPeer::GRADE_ID, $this->id);

			if (!isset($this->lastGradeSubjectCriteria) || !$this->lastGradeSubjectCriteria->equals($criteria)) {
				$this->collGradeSubjects = GradeSubjectPeer::doSelectJoinSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastGradeSubjectCriteria = $criteria;

		return $this->collGradeSubjects;
	}

	/**
	 * Clears out the collItemGrades collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addItemGrades()
	 */
	public function clearItemGrades()
	{
		$this->collItemGrades = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collItemGrades collection (array).
	 *
	 * By default this just sets the collItemGrades collection to an empty array (like clearcollItemGrades());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initItemGrades()
	{
		$this->collItemGrades = array();
	}

	/**
	 * Gets an array of ItemGrade objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Grade has previously been saved, it will retrieve
	 * related ItemGrades from storage. If this Grade is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ItemGrade[]
	 * @throws     PropelException
	 */
	public function getItemGrades($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collItemGrades === null) {
			if ($this->isNew()) {
			   $this->collItemGrades = array();
			} else {

				$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

				ItemGradePeer::addSelectColumns($criteria);
				$this->collItemGrades = ItemGradePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

				ItemGradePeer::addSelectColumns($criteria);
				if (!isset($this->lastItemGradeCriteria) || !$this->lastItemGradeCriteria->equals($criteria)) {
					$this->collItemGrades = ItemGradePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastItemGradeCriteria = $criteria;
		return $this->collItemGrades;
	}

	/**
	 * Returns the number of related ItemGrade objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ItemGrade objects.
	 * @throws     PropelException
	 */
	public function countItemGrades(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collItemGrades === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

				$count = ItemGradePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

				if (!isset($this->lastItemGradeCriteria) || !$this->lastItemGradeCriteria->equals($criteria)) {
					$count = ItemGradePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collItemGrades);
				}
			} else {
				$count = count($this->collItemGrades);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ItemGrade object to this object
	 * through the ItemGrade foreign key attribute.
	 *
	 * @param      ItemGrade $l ItemGrade
	 * @return     void
	 * @throws     PropelException
	 */
	public function addItemGrade(ItemGrade $l)
	{
		if ($this->collItemGrades === null) {
			$this->initItemGrades();
		}
		if (!in_array($l, $this->collItemGrades, true)) { // only add it if the **same** object is not already associated
			array_push($this->collItemGrades, $l);
			$l->setGrade($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Grade is new, it will return
	 * an empty collection; or if this Grade has previously
	 * been saved, it will retrieve related ItemGrades from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Grade.
	 */
	public function getItemGradesJoinItem($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collItemGrades === null) {
			if ($this->isNew()) {
				$this->collItemGrades = array();
			} else {

				$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

				$this->collItemGrades = ItemGradePeer::doSelectJoinItem($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ItemGradePeer::GRADE_ID, $this->id);

			if (!isset($this->lastItemGradeCriteria) || !$this->lastItemGradeCriteria->equals($criteria)) {
				$this->collItemGrades = ItemGradePeer::doSelectJoinItem($criteria, $con, $join_behavior);
			}
		}
		$this->lastItemGradeCriteria = $criteria;

		return $this->collItemGrades;
	}

	/**
	 * Clears out the collContractGrades collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addContractGrades()
	 */
	public function clearContractGrades()
	{
		$this->collContractGrades = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collContractGrades collection (array).
	 *
	 * By default this just sets the collContractGrades collection to an empty array (like clearcollContractGrades());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initContractGrades()
	{
		$this->collContractGrades = array();
	}

	/**
	 * Gets an array of ContractGrade objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Grade has previously been saved, it will retrieve
	 * related ContractGrades from storage. If this Grade is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ContractGrade[]
	 * @throws     PropelException
	 */
	public function getContractGrades($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collContractGrades === null) {
			if ($this->isNew()) {
			   $this->collContractGrades = array();
			} else {

				$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

				ContractGradePeer::addSelectColumns($criteria);
				$this->collContractGrades = ContractGradePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

				ContractGradePeer::addSelectColumns($criteria);
				if (!isset($this->lastContractGradeCriteria) || !$this->lastContractGradeCriteria->equals($criteria)) {
					$this->collContractGrades = ContractGradePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastContractGradeCriteria = $criteria;
		return $this->collContractGrades;
	}

	/**
	 * Returns the number of related ContractGrade objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ContractGrade objects.
	 * @throws     PropelException
	 */
	public function countContractGrades(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collContractGrades === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

				$count = ContractGradePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

				if (!isset($this->lastContractGradeCriteria) || !$this->lastContractGradeCriteria->equals($criteria)) {
					$count = ContractGradePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collContractGrades);
				}
			} else {
				$count = count($this->collContractGrades);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ContractGrade object to this object
	 * through the ContractGrade foreign key attribute.
	 *
	 * @param      ContractGrade $l ContractGrade
	 * @return     void
	 * @throws     PropelException
	 */
	public function addContractGrade(ContractGrade $l)
	{
		if ($this->collContractGrades === null) {
			$this->initContractGrades();
		}
		if (!in_array($l, $this->collContractGrades, true)) { // only add it if the **same** object is not already associated
			array_push($this->collContractGrades, $l);
			$l->setGrade($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Grade is new, it will return
	 * an empty collection; or if this Grade has previously
	 * been saved, it will retrieve related ContractGrades from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Grade.
	 */
	public function getContractGradesJoinContract($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(GradePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collContractGrades === null) {
			if ($this->isNew()) {
				$this->collContractGrades = array();
			} else {

				$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

				$this->collContractGrades = ContractGradePeer::doSelectJoinContract($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ContractGradePeer::GRADE_ID, $this->id);

			if (!isset($this->lastContractGradeCriteria) || !$this->lastContractGradeCriteria->equals($criteria)) {
				$this->collContractGrades = ContractGradePeer::doSelectJoinContract($criteria, $con, $join_behavior);
			}
		}
		$this->lastContractGradeCriteria = $criteria;

		return $this->collContractGrades;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collGradeSubjects) {
				foreach ((array) $this->collGradeSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collItemGrades) {
				foreach ((array) $this->collItemGrades as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collContractGrades) {
				foreach ((array) $this->collContractGrades as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collGradeSubjects = null;
		$this->collItemGrades = null;
		$this->collContractGrades = null;
			$this->aDegree = null;
			$this->aTimetable = null;
			$this->aCurso = null;
			$this->aParalelo = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseGrade:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseGrade::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseGrade
