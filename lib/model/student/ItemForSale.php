<?php


/**
 * Skeleton subclass for representing a row from the 'sch_item_for_sale' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Fri Sep 28 00:21:35 2012
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model.student
 */
class ItemForSale extends BaseItemForSale {
  function getTotalPrice()
  {
    return ($this->getQuantity() * $this->getPrice());
  }
  
  function getTotalPriceDiscount()
  {
     $q = (double)$this->getQuantity();
     $p = (double)$this->getPrice();
     $d = (double)$this->getDiscount();

     // return numbers::my_format_number_calc($q * ($p - $d));
     return ($q * ($p - $d));
  }
  
  function getTotalDiscount()
  {
     return ($this->getQuantity() * $this->getDiscount());
  }

  function getAveragePrice($item_price = 0, $quantity = 1)
  {
    return (($this->getQuantity() * $this->getPrice()) + ($item_price * $quantity)) / ($this->getQuantity() + $quantity);
  }
  
} // ItemForSale